#ifndef SEARCH_VEC_HPP
#define SEARCH_VEC_HPP

#include<vector>
#include <random>
#include <iostream>

using namespace std;

class SearchVec
{
    public:

static void showvec(vector<int> vec); 
static int binarysearch(vector<int> &vec,int value);


private:
    /* data */
public:
    SearchVec(/* args */);
    ~SearchVec();
};





#endif 